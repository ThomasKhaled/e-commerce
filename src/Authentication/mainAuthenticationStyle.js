import { styled } from "@mui/system";

const mainAuthenticationStyle = styled((theme) => ({
  inputContainer: {
    color: "white",
    fontSize: "1.9rem",
    borderColor: "white",
  },
}));

export default mainAuthenticationStyle;
